import { LogicalCart, LogicalCartScore } from './interfaces';

export function perfectCart(
    observed: LogicalCart,
    expected: LogicalCart
): LogicalCartScore {
    throw 0;
}

export function completeCart(
    observed: LogicalCart,
    expected: LogicalCart
): LogicalCartScore {
    throw 0;
}

export function repairCost(
    observed: LogicalCart,
    expected: LogicalCart
): LogicalCartScore {
    throw 0;
}
